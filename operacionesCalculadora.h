/** \brief Suma dos numeros.
 *
 * \param adquiere un numero.
 * \param adquiere otro numero.
 * \return devuelve la suma de los dos numeros.
 *
 */

int sumar (int num1,int num2);

/** \brief resta dos numeros.
 *
 * \param adquiere un numero.
 * \param adquiere otro numero.
 * \return devuelve la resta de los dos numeros.
 *
 */

int restar (int num1,int num2);

/** \brief multiplica dos numeros.
 *
 * \param adquiere un numero.
 * \param adquiere otro numero.
 * \return devuelve la multiplicacion de los dos numeros.
 *
 */

int multip (int num1,int num2);

/** \brief dividir dos numeros.
 *
 * \param adquiere un numero.
 * \param adquiere otro numero.
 * \return devuelve la divicion de los dos numeros.
 *
 */

int divid (int num1,int num2);

/** \brief Calcula el factorial de un numero
 *
 * \param Recibe un numero
 *
 * \return Devuelve el numero expresado en su forma factorial
 *
 */

 int factorial (int num1);

